{
  "name": "api-meli",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "build": "yarn clean && NODE_ENV=production tsc",
    "clean": "rm -rf build/",
    "compile": "tsc && yarn start",
    "dev": "NODE_ENV=development nodemon --watch src -e ts,ejs --exec yarn compile",
    "start": "NODE_PATH=./build node build/server.js",
    "lint": "eslint src/",
    "test": "kcd-scripts test"
  },
  "dependencies": {
    "apollo-datasource-rest": "^0.13.0",
    "apollo-server": "^2.23.0",
    "apollo-server-testing": "^2.23.0",
    "graphql": "^15.5.0",
    "ts-jest": "^26.5.5",
    "winston": "^3.3.3"
  },
  "devDependencies": {
    "@types/jest": "^26.0.23",
    "eslint-plugin-prettier": "^3.4.0",
    "jest": "^26.6.3",
    "kcd-scripts": "^10.0.0",
    "nodemon": "^2.0.7",
    "typescript": "^4.2.4"
  }
}